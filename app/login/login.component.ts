import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'login',
    template: `
        <p>Login</p>
        <button>click</button>
    `
})

export class LoginComponent { }
